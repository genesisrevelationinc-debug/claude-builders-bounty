name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -e .

      - name: Get PR diff
        id: get-diff
        run: |
          curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}" \
            | jq -r '.diff_url' | xargs curl -s > pr.diff
          echo "diff_file=pr.diff" >> $GITHUB_OUTPUT

      - name: Run Claude Review
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          claude-review --pr ${{ github.event.pull_request.html_url }} --diff ${{ steps.get-diff.outputs.diff_file }} --post-comment
--- /dev/null
"""Claude Code PR Review Agent."""

__version__ = "0.1.0"

from .reviewer import ClaudeReviewer, ReviewResult
"""Core PR review logic using Claude API."""

import json
import os
import re
import subprocess
import sys
from dataclasses import dataclass
from pathlib import Path
from typing import Optional

import requests


@dataclass
class ReviewResult:
    """Structured review result."""

    summary: str
    risks: list[str]
    suggestions: list[str]
    confidence: str
    raw_response: str

    def to_markdown(self) -> str:
        """Convert review to structured Markdown."""
        lines = [
            "## 🤖 Claude Code PR Review",
            "",
            "### Summary",
            "",
            self.summary,
            "",
            "### Identified Risks",
            "",
        ]
        for risk in self.risks:
            lines.append(f"- {risk}")
        lines.extend(["", "### Improvement Suggestions", ""])
        for suggestion in self.suggestions:
            lines.append(f"- {suggestion}")
        lines.extend(["", f"**Confidence Score:** {self.confidence}", ""])
        return "\n".join(lines)


class ClaudeReviewer:
    """PR reviewer using Claude API."""

    API_URL = "https://api.anthropic.com/v1/messages"

    def __init__(self, api_key: Optional[str] = None):
        self.api_key = api_key or os.environ.get("ANTHROPIC_API_KEY")
        if not self.api_key:
            raise ValueError("ANTHROPIC_API_KEY required")

    def _call_claude(self, prompt: str) -> str:
        """Call Claude API with the given prompt."""
        headers = {
            "x-api-key": self.api_key,
            "Content-Type": "application/json",
            "anthropic-version": "2023-06-01",
        }
        payload = {
            "model": "claude-sonnet-4-20250514",
            "max_tokens": 4096,
            "messages": [
                {
                    "role": "user",
                    "content": prompt,
                }
            ],
        }
        response = requests.post(self.API_URL, headers=headers, json=payload, timeout=120)
        response.raise_for_status()
        return response.json()["content"][0]["text"]

    def _parse_review(self, raw_response: str) -> ReviewResult:
        """Parse Claude's response into structured review."""
        # Try to extract JSON from response
        json_match = re.search(r"