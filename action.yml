name: 'Claude Code Review'
description: 'Reviews PRs with Claude Code and posts structured comments'
author: 'Claude Builder'
inputs:
  github-token:
    description: 'GitHub token for API access'
    required: true
  claude-token:
    description: 'Claude Code API token'
    required: true
runs:
  using: 'composite'
  steps:
    - name: Get PR diff
      run: |
        PR_NUMBER=$(echo $GITHUB_REF | sed 's/refs\/pull\///' | sed 's/\/merge//')
        echo "PR_NUMBER=$PR_NUMBER" >> $GITHUB_ENV
    - name: Analyze with Claude Code
      run: |
        # Here you would call the Claude Code API with the PR diff
        # and generate the structured review comment
      shell: bash