# /generate-changelog

Generate a structured `CHANGELOG.md` from git history.

## Description

This skill fetches commits since the last git tag, auto-categorizes them into
`Added` / `Fixed` / `Changed` / `Removed`, and outputs a properly formatted
`CHANGELOG.md`.

## Usage

