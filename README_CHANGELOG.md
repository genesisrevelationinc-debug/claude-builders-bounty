# Generate Changelog

This skill generates a structured `CHANGELOG.md` from a project's git history.

## Setup

1. Make the script executable: `chmod +x changelog.sh`
2. Navigate to your git repository
3. Run: `bash changelog.sh`

The script will generate a `CHANGELOG.md` file in the current directory with changes since the last git tag.